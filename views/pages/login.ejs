<%- include('../partials/header') %>

<style>
  /* body {
    background-color: #006dbd;
    background: linear-gradient(90deg, rgba(0, 26, 45, 1) 0%, rgba(35, 133, 204, 1) 50%, rgba(0, 109, 189, 1) 100%);
  } */
  /* .container-lg {
    background-image: url('/assets/img/Flyers-Brochure-Test-mage.jpg');
    background-repeat: no-repeat;
    background-attachment: fixed;
  } */

  /* .back-btn {
    position: absolute;
    display: flex;
    font-size: 46px;
    transform: translateX(60px) translateY(-10px);
    padding: 10px;
  }

  a.back-btn:hover {
    background-color: white;
    color: black !important;
    border-radius: 5px;
  }


  a.back-btn:active {
    box-shadow: inset 0 0 25px #000000c4;
  } */
</style>

<body>
  <!-- <a href="/" class="back-btn link-light">
    <i class="fa-solid fa-circle-left"></i> 
    <span class="fs-4 ms-3 align-self-center">กลับไปหน้าแรก</span>
  </a> -->
  <div class="container-lg mb-5 ">
    <div class="mt-3">
      <% if(success_msg != "") { %>
        <% success_msg.forEach((item) => { %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
            <strong><%- item %></strong> 
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% }); %>
        <% }else if(errors != "") {%> warnings
        <% errors.forEach((item) => { %>
            <div class="alert alert-danger alert-dismissible fade show" role="alert">
                <strong><%- item %></strong> 
                <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
            </div>
        <% }); %>
        <% }else if(warnings != ""){ %>
          <% warnings.forEach((item) => { %>
          <div class="alert alert-warning alert-dismissible fade show" role="alert">
            <strong><%- item %></strong> 
            <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
        <% }); %>

        <% } %>
    </div>
    <div class="d-flex">
      <div class="login card py-5 mt-2">
        <form method="post" action="/login" class="m-auto needs-validation" novalidate>
          <div class="w-25 m-auto">
            <img src="/assets/img/logo.png" alt="clipartpress" width="10">
          </div>
          <h3 class="mb-5 text-center">เข้าสู่ระบบ</h3>
          <!-- username input -->
          <div class="form-outline mb-4">
            <input type="text" id="form1Example1" class="form-control" name="username" value="test" required/>
            <label class="form-label" for="form1Example1">ชื่อผู้ใช้</label>
          </div>

          <!-- Password input -->
          <div class="form-outline mb-4">
            <input type="password" id="form1Example2" class="form-control" name="password" value="123456" required/>
            <label class="form-label" for="form1Example2">รหัสผ่าน</label>
          </div>

          <!-- 2 column grid layout for inline styling -->
          <div class="row mb-4">
            <div class="col">
              <!-- Simple link -->
              <a href="/identify">ลืมรหัสผ่าน?</a>
            </div>
          </div>

          <!-- Submit button -->
          <button type="submit" class="btn btn-primary btn-block">เข้าสู่ระบบ</button>

          <div class="text-center mt-5">
            <p>ยังไม่มีบัญชี? <a href="/register">สมัครสมาชิก</a></p>
          </div>
        </form>
        <!-- <div class="line-or">
        </div>
        <div class="other-login ">
          <button class="login-fb ">
            <p>เข้าสู่ระบบด้วย Facebook</p>
          </button>
          <button class="login-google d-flex">
            <img src="assets/img/googleicon.png" alt="google icon" width="20" class="align-self-center"
              style="margin-right: 10px;">
            <p class="align-self-center">เข้าสู่ระบบด้วย Google</p>
          </button>
        </div> -->
      </div>
    </div>
  </div>
  <script src="/js/validateBs.js"></script>
  <script>
            document.querySelectorAll('.form-outline').forEach((formOutline) => {
            new mdb.Input(formOutline).init();
        });
  </script>
</body>
